" Bundles that are only installed with the 'mac' rcm tag

" Plug 'Valloric/YouCompleteMe', { 'do': './install.py --clang-completer' }
Plug 'davidhalter/jedi-vim', { 'do': 'git submodule update --init' }
